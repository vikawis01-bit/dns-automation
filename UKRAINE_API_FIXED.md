# ✅ Исправление API ukraine.com.ua

## 🔧 Что было исправлено:

### 1. Правильный базовый URL:
- **Было:** `https://www.ukraine.com.ua/api/v2` (неправильно)
- **Стало:** `https://adm.tools/action/` ✅

### 2. Правильный формат запросов:
- **Было:** REST API с JSON
- **Стало:** POST запросы с `http_build_query()` и GET параметрами ✅

### 3. Правильные endpoints:
- `dns/record_list` - получение списка DNS записей
- `dns/record_edit` - редактирование записи
- `dns/record_add` - добавление записи
- `dns/record_delete` - удаление записи
- `dns/nameservers_set` - установка NS записей

### 4. Правильная структура данных:
- `subdomain_id` - ID записи (для редактирования/удаления)
- `subdomain` - имя поддомена (пустая строка для корня домена @)
- `data` - содержимое записи (IP для A записи)
- `type` - тип записи (A, AAAA, CNAME и т.д.)
- `ttl` - время жизни записи
- `priority` - приоритет (для MX записей)

### 5. Правильная авторизация:
- Формат: `Authorization: Bearer {token}`
- Content-Type: `application/x-www-form-urlencoded`

---

## 📝 Пример использования:

### Получение DNS записей:
```
POST https://adm.tools/action/dns/record_list/?domain=example.com
Headers: Authorization: Bearer {token}
Data: (пусто)
```

### Редактирование DNS записи:
```
POST https://adm.tools/action/dns/record_edit/?domain=example.com
Headers: Authorization: Bearer {token}
Data: subdomain_id=123&type=A&subdomain=&data=192.168.1.1&ttl=3600&priority=0
```

---

## ✅ После обновления на Render:

1. Подождите 2-3 минуты пока обновится
2. Откройте приложение
3. В настройках API URL уже будет правильный: `https://adm.tools/action`
4. Вставьте ваш токен (из панели управления)
5. Сохраните и попробуйте снова!

---

## 🔑 Где взять токен:

1. Зайдите в панель управления ukraine.com.ua
2. Раздел **"API"** → **"Данные доступа"**
3. Нажмите **"Активировать токен"**
4. Скопируйте токен (показывается только один раз!)

---

**Теперь всё должно работать правильно!** 🚀

