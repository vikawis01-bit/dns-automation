# 🚀 Быстрый деплой на Render.com (Бесплатно)

## Шаг 1: Подготовка GitHub репозитория

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit - DNS Automation App"

# Создайте репозиторий на GitHub и добавьте remote
git remote add origin https://github.com/ВАШ_USERNAME/dns-automation.git
git branch -M main
git push -u origin main
```

## Шаг 2: Создайте аккаунт на Render.com

1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub (самый простой способ)
3. Подтвердите email

## Шаг 3: Создайте Web Service

1. Нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий
3. Выберите репозиторий `dns-automation`
4. Настройте:
   - **Name:** `dns-automation` (или любое другое)
   - **Region:** выберите ближайший
   - **Branch:** `main`
   - **Root Directory:** оставьте пустым
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `gunicorn app:app`

## Шаг 4: Настройте переменные окружения

В разделе **Environment** добавьте:

```
CLOUDFLARE_API_TOKEN=ваш_cloudflare_token
REGISTRAR_API_URL=https://api.ukraine.com.ua/v2
REGISTRAR_API_KEY=ваш_ukraine_api_key
REGISTRAR_API_SECRET=ваш_ukraine_api_secret
```

**Важно:** Не добавляйте `.env` файл в git! Используйте только переменные окружения в Render.

## Шаг 5: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь завершения деплоя (обычно 2-5 минут)
3. Ваше приложение будет доступно по адресу: `https://dns-automation.onrender.com`

## ✅ Готово!

Теперь вы можете:
- Открыть приложение в браузере
- Протестировать все этапы автоматизации
- При необходимости подключить свой домен (в настройках Render)

---

## 🔧 Альтернативы

### Railway.app (еще проще!)

1. Зайдите на https://railway.app
2. **New Project** → **Deploy from GitHub repo**
3. Выберите репозиторий
4. Railway автоматически определит Python проект
5. Добавьте переменные окружения в **Variables**
6. Готово! 🎉

### PythonAnywhere (для тестирования)

1. Зайдите на https://www.pythonanywhere.com
2. Создайте бесплатный аккаунт
3. Загрузите файлы через **Files** tab
4. Создайте **Web app** → **Manual configuration**
5. Укажите путь: `/home/ваш_username/dns-automation/app.py`
6. Настройте переменные окружения в **Web** → **Environment variables**

---

## ⚠️ Важные замечания

- **Бесплатный tier на Render:** приложение "засыпает" после 15 минут неактивности
- **Первый запрос** после пробуждения может быть медленным (30-60 сек)
- Для постоянной работы рассмотрите платный план ($7/месяц) или VPS

---

## 🐛 Решение проблем

**Ошибка при деплое:**
- Проверьте, что все зависимости в `requirements.txt`
- Убедитесь, что `gunicorn` установлен
- Проверьте логи в Render Dashboard

**Приложение не запускается:**
- Проверьте переменные окружения
- Убедитесь, что `Start Command` правильный: `gunicorn app:app`
- Проверьте логи в разделе **Logs**

