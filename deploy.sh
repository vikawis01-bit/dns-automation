#!/bin/bash

# Скрипт для быстрого развертывания приложения

echo "🚀 Начало развертывания DNS и Cloudflare Автоматизации..."

# Проверка Python
if ! command -v python3 &> /dev/null; then
    echo "❌ Python3 не установлен!"
    exit 1
fi

# Создание виртуального окружения
if [ ! -d "venv" ]; then
    echo "📦 Создание виртуального окружения..."
    python3 -m venv venv
fi

# Активация виртуального окружения
echo "🔧 Активация виртуального окружения..."
source venv/bin/activate

# Установка зависимостей
echo "📥 Установка зависимостей..."
pip install --upgrade pip
pip install -r requirements.txt

# Проверка .env файла
if [ ! -f ".env" ]; then
    echo "⚠️  Файл .env не найден. Создаю из примера..."
    cp .env.example .env
    echo "📝 Пожалуйста, отредактируйте .env файл и добавьте ваши API ключи!"
    exit 1
fi

echo "✅ Развертывание завершено!"
echo ""
echo "Для запуска приложения:"
echo "  source venv/bin/activate"
echo "  python app.py"
echo ""
echo "Или для продакшена с Gunicorn:"
echo "  pip install gunicorn"
echo "  gunicorn -w 4 -b 0.0.0.0:5000 app:app"

