# 🌐 Поддерживаемые регистраторы

## ✅ Уже есть примеры кода:

### 1. **Ukraine.com.ua** ⭐ (текущая реализация)
- ✅ Полностью реализовано
- Файл: `ukraine_registrar.py`
- Используется по умолчанию

### 2. **Namecheap**
- ✅ Есть примеры кода
- Файл: `registrar_examples.py`
- Формат: XML API через параметры запроса
- Нужно: ApiUser, ApiKey, UserName, ClientIp

### 3. **GoDaddy**
- ✅ Есть примеры кода
- Файл: `registrar_examples.py`
- Формат: REST API с sso-key авторизацией
- Нужно: API Key + API Secret

### 4. **Name.com**
- ✅ Есть примеры кода
- Файл: `registrar_examples.py`
- Формат: REST API v4 с Basic Auth
- Нужно: API Key + API Secret

---

## 🔧 Как добавить поддержку другого регистратора:

### Вариант 1: Использовать существующие примеры

1. Откройте `registrar_examples.py`
2. Найдите пример для вашего регистратора
3. Скопируйте функции
4. Замените функции в `ukraine_registrar.py` на функции из примера

### Вариант 2: Добавить новый регистратор

1. Создайте новый файл `namecheap_registrar.py` (или другой)
2. Скопируйте структуру из `ukraine_registrar.py`
3. Адаптируйте функции под API вашего регистратора
4. Обновите `app.py` для использования нового модуля

---

## 📋 Что нужно для интеграции:

Для любого регистратора нужны функции:
- `get_dns_records(domain)` - получение DNS записей
- `delete_dns_record(domain, record_id)` - удаление записи
- `create_dns_record(domain, type, name, content, ttl)` - создание записи
- `update_nameservers(domain, nameservers)` - обновление NS

---

## 🚀 Быстрое добавление Namecheap:

Если хотите использовать Namecheap вместо Ukraine.com.ua:

1. **Создайте файл `namecheap_registrar.py`** на основе `ukraine_registrar.py`
2. **Замените функции** на примеры из `registrar_examples.py`
3. **Обновите `app.py`** - замените импорт:
   ```python
   from namecheap_registrar import (
       namecheap_get_dns_records,
       namecheap_set_dns_records,
       ...
   )
   ```
4. **Обновите веб-интерфейс** - измените поля для ввода:
   - ApiUser
   - ApiKey
   - UserName
   - ClientIp (IP вашего сервера)

---

## 📚 Другие популярные регистраторы:

Могу добавить поддержку для:
- **Cloudflare Registrar** (если домены там)
- **Google Domains**
- **NameSilo**
- **Porkbun**
- **Dynadot**
- **Hover**
- И любых других с REST API или XML API

---

## 💡 Универсальный подход:

Если регистратор использует стандартный REST API:
- Используйте функции из раздела "Generic REST API" в `registrar_examples.py`
- Адаптируйте только URL и формат авторизации

---

**Хотите добавить Namecheap или другой регистратор? Скажите, и я помогу!** 🚀

